---
type: 网络安全主题
related_to: "[[01-001-二进制安全概览与学习路线]]"
难度: 入门
前置知识: 01-001
对应语言等级: 精通级
status: Active
---

# 01-002 x86-64 汇编与栈帧基础

## 基础信息
- 难度：入门
- 前置知识：[[01-001-二进制安全概览与学习路线]]
- 对应语言等级：精通级

## 核心原理
x86-64 是 Intel 64 位指令集架构，向下兼容 x86。程序调用流程依赖调用约定：System V AMD64 ABI 规定前六个整数/指针参数依次通过 rdi、rsi、rdx、rcx、r8、r9 传递，浮点参数通过 xmm0-xmm7，多余参数压栈；返回地址由 `call` 指令压入栈顶，由 `ret` 指令弹出。栈帧是每次函数调用分配的一段栈空间，用于保存局部变量、保存的寄存器与被调用的返回地址。栈向下增长（地址递减），`rsp` 指向栈顶，`rbp` 常作为帧基址（优化编译时可能省略）。

权威依据：System V Application Binary Interface, AMD64 Architecture Processor Supplement 定义了参数寄存器与边界对齐规则；Intel SDM 规定 `call`/`ret` 的内存语义。

## 技术要点
- 寄存器：rax（返回值/被除数）、rbx（被调用者保存）、rcx/rdx（第3/4参数，除法余数）、rsi/rdi、rbp/rsp、r8-r15；rip 指指令指针（相对寻址基础）。
- 栈布局（无优化典型帧）：返回地址（高位）→ 保存的 rbp → 局部变量 → 栈顶（低地址，rsp 指向）。
- 关键指令：`push/pop`（改 rsp 并读写内存）、`call`（push 返回地址+跳转）、`ret`（pop 回 rip）、`lea`（地址运算）、`movsz/movzx`（符号/零扩展）。
- 字节序：x86-64 为小端（little-endian），多字节数低字节在低地址。
- frame pointer omission（-O2 起默认）下仅用 rsp 定位局部变量，`rbp` 可能不再指向帧底。

## 实践示例
环境：Ubuntu 22.04、GCC 11.4、GDB 12.1。

将以下 C 文件以 `gcc -O0 -fno-stack-protector -fno-pie -o test test.c` 编译并用 GDB 反汇编：
```bash
gcc -g -O0 -fno-stack-protector -fno-pie -o test test.c
gdb -q ./test
(gdb) disassemble main
(gdb) break main
(gdb) run
(gdb) info registers rip rsp rbp
(gdb) x/8gx $rsp
```
`disassemble main` 即可看到 `push %rbp; mov %rsp,%rbp` 的典型序言与 `leave; ret` 结语。

## 常见问题与误区
- 误区一：以为栈地址向上增长。x86-64 栈向下增长，栈溢出覆盖目标通常位于当前帧的上方（更高地址）。
- 误区二：混淆「被调用者保存」与「调用者保存」寄存器。跨调用存活的数据需放到 rbx/rbp/r12-r15 等被调用者保存寄存器中，否则应在调用前自行保存。
- 误区三：用 `gdb x/Ndw` 读 64 位数据时误用 32 位视图，导致地址被截断。读指针应使用 `x/Ngx`。

## 参考来源
- System V Application Binary Interface AMD64 Supplement v1.0 - ABI 标准 - https://gitlab.com/x86-psABIs/x86-64-ABI
- Intel 64 and IA-32 Architectures Software Developer's Manual, Vol.1 - 官方手册 - https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html

## 合规声明
本文仅用于授权安全测试与学习研究，未经授权对他人系统进行测试属于违法行为。